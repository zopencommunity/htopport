diff --git c/Compat.c w/Compat.c
index 6df0b08..71b2ee9 100644
--- c/Compat.c
+++ w/Compat.c
@@ -154,3 +154,11 @@ ssize_t Compat_readlink(openat_arg_t dirfd,
 
    return readlink(linkPath, buf, bufsize);
 }
+
+#ifndef HAVE_STPCPY
+char* stpcpy(char* restrict dest, const char* restrict src) {
+   size_t len = strlen(src);
+   memcpy(dest, src, len + 1);
+   return dest + len;
+}
+#endif
